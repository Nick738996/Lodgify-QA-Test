{
  "name": "lodgify-automation-qa-test",
  "description": "Lodgify QA automation challenge",
  "version": "1.0.0",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GilbertoGNG/Lodgify-Automation-QA-Test.git"
  },
  "author": "Lodgify QA Team",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^9.5.3"
  },
  "scripts": {
    "dev-server": "node server/server.js",
    "install-all": "npm install && cd server && npm install",
    "cy:open:qa": "npx cypress open",
    "cy:run:qa": "npx cypress run --browser chrome",
    "cy:open:dev": "npx cypress open --config-file cypress.dev.json",
    "cy:run:dev": "npx cypress run --browser chrome --config-file cypress.dev.json"
  }
}
